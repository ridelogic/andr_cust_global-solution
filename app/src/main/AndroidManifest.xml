<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Newproject"
        tools:targetApi="31">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.register.RegisterActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.otp.OtpVerificationActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.otp.CustomerOtpRegistrationActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.common.PlaceholderActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.myservices.MyServicesActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.booking.ServiceCategoryActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.booking.ProductSelectionActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.booking.ProblemSelectionActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.booking.AddressSelectionActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.booking.AddAddressActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.booking.DateTimeSelectionActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />

        <activity
            android:name=".ui.booking.ConfirmBookingActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Newproject" />
    </application>

</manifest>